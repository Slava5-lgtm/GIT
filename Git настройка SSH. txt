Настойка SSH
Github → настройки → SSH and GPG keys → Терминал (Windows Bash) → ввести команду ssh-keygen нажимаем Enter до появления седевшего изображения

                                                            Linux

Далее переходим в Github -> SSH and GPG keys → New SSH key → Titie (вводим имя) → Далее  файлы  →скрытые файлы → .ssh → id_rsa.pub (открываем любым блокнотом) → Копируем → и Вставляем в Key.

                                                            Windows

github -> коючи SSH GPG -> Добовляеи новый ключ (название) -> Проводник -> Этот компьютер Локальный диск (С) -> Плоьзователи -> Имя пользователя -> .ssh -> id_rsa.pud (любмым блокноам
 отераваем) -> Копируем -> Переходим в github -> New SSH key (вставляем) -> Add SSH key 

Все я смогу клонировать репозиторий .
VSC
git clone (и ссылка ssh) — клонирую репозиторий, перехожу в этот репозиторий 
cd (имя репозитория который клонироваля) работаю в нем
git add **
git commit -m "+"
git push


ПЕРЕХОДИМ В НУЖНЫЙ РЕПОЗИТОРИЙ КОМАНДОЙ В ДАНОМ СЛУЧАЕ cd ~/Final-control-work/Git СОХРАНЯЕМ КОМЕНТИРУЕМ ОТПРОВЛЯЕМ НАУДОЛЕНЫЙ
gft trtuyu