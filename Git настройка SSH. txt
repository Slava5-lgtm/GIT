Настойка SSH
Github → настройки → SSH and GPG keys → Терминал (Windows Bash) → ввести команду ssh-keygen нажимаем Enter до появления седевшего изображения

Далее переходим в Github -> SSH and GPG keys → New SSH key → Titie (вводим имя) → Далее  файлы  →скрытые файлы → .ssh → id_rsa.pub (открываем любым блокнотом) → Копируем → и Вставляем в Key.

Все я смогу клонировать репозиторий .
VSC
git clone (и ссылка ssh) — клонирую репозиторий
cd (имя репозитория который клонироваля)
git push 
